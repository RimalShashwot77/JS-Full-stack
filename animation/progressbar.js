window.addEventListener('load', () => {
    const progressBar = document.querySelector('.progress-bar');
    setTimeout(() => {
        progressBar.style.width = '100%'; // Adjust the width as needed
    }, 1000); // Delay to simulate loading
    
});
